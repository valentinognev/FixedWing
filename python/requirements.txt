# Balloon-race / SITL Python helpers (install under python/)
numpy>=1.24
pyzmq>=25
opencv-python>=4.8,<5
pymavlink>=2.4
mss>=9.0
matplotlib>=3.7
# Camera pane uses cv2.namedWindow / imshow (not the headless build).
